#ifndef BOARD_H
#define BOARD_H
#include "types.h"

extern Square board[40]; // Assuming a board with 40 squares
extern Event cards[20]; // Assuming a deck with 20 event cards

#endif // BOARD_H
